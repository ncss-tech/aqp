# Detect color specification from a vector of values, or a matrix of color coordinates

Detect color specification from a vector of values, or a matrix of color
coordinates

## Usage

``` r
.detectColorSpec(col)
```

## Arguments

- col:

  character vector, numeric matrix with 3 columns, or data.frame with 3
  columns
