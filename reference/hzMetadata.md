# Get horizon-level metadata

Get `idname(object)` and `hzidname(object)`, with `hzdesgnname(object)`,
`hztexclname(object)` (if defined)

## Usage

``` r
# S4 method for class 'SoilProfileCollection'
hzMetadata(object)
```

## Arguments

- object:

  a SoilProfileCollection
