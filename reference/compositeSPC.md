# Return a list representation of site and horizon level data

`compositeSPC()` is a convenience function that returns a named list
representation of the columns from the `@site` and `@horizons` slots.

## Usage

``` r
compositeSPC(object)
```

## Arguments

- object:

  A SoilProfileCollection

## Value

A list.

## Author

Andrew G. Brown.
