# Store groupings within a profile collection.

Store groupings within a profile collection.

## Usage

``` r
groupSPC(object, ...)
```

## Arguments

- object:

  SoilProfileCollection.

- ...:

  One or more expressions evaluated within the context of `object` that
  resolve to vectors that can be coerced to factor "groups."
